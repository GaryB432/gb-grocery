language: node_js
dist: trusty
node_js:
  - "stable"
  - "6.9"

addons:
  chrome: stable

branches:
  only:
    - master

before_script:
  # - npm install -g firebase-tools
  - npm install -g @angular/cli

script:
  - ng test --single-run

# after_success:
#   - firebase deploy --token $FIREBASE_TOKEN

notifications:
  email:
    on_failure: change
    on_success: change