import { LikerDirective } from './liker.directive';

describe('LikerDirective', () => {
  it('should create an instance', () => {
    const directive = new LikerDirective();
    expect(directive).toBeTruthy();
  });
});
